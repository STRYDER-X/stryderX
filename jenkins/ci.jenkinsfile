pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile.dev'
            dir 'stryderx_docker'
            reuseNode true
        }
    }

    stages {
        stage('Verify Repo Copied Over') {
            steps {
                sh 'pwd'
                sh 'ls -ln'
            }
        }
    }
}
