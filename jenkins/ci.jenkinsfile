pipeline {
    agent {
        dockerfile {
            filename 'stryderx_docker/Dockerfile.dev'
            dir '.'
            reuseNode true
        }
    }

    stages {
        stage('Verify Repo Copied Over') {
            steps {
                sh 'pwd'
                sh 'ls -ln'
            }
        }
    }
}
